export class User {
    constructor(
        mobileNumber: string,
        emailAddress: string,
        firstName: string,
        lastName: string,
        jwt: string,
        userId: string,
    ){}
}
